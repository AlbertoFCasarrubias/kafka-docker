services:
  - type: web
    name: zookeeper
    env: docker
    plan: free
    autoDeploy: false
    dockerfilePath: zookeeper/Dockerfile

  - type: web
    name: kafka
    env: docker
    plan: free
    autoDeploy: false
    dockerfilePath: kafka/Dockerfile
